#include <iostream>

#include "../include/xgraph.hpp"

/*
auto complete_graph_test (const int n) {
  using xgraph::MyNode;
  using xgraph::MyEdge;

  auto graph = std::make_shared<xgraph::Graph<>>();

  // Add nodes
  for (int i = 0; i < n; ++i) {
    graph->AddNode(
        std::make_shared<MyNode>(std::to_string(i))
    );
  }

  // Add edges
  for (int i = 0; i < n; ++i) {
    for (int j = i + 1; j < n; ++j) {
    }
  }

  return graph;
}

int main_2() {
  auto g = complete_graph_test(5);

  auto g_copy = xgraph::Graph(*g);

  std::cout << "Test Succeed!" << std::endl;
}
*/
